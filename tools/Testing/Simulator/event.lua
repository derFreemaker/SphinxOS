return function()
    event = {}

    ---@diagnostic disable-next-line
    function event.clear()
    end

    ---@diagnostic disable-next-line
    function event.ignore(component)
    end

    ---@diagnostic disable-next-line
    function event.ignoreAll()
    end

    ---@diagnostic disable-next-line
    function event.listen(component)
    end

    ---@diagnostic disable-next-line
    function event.listening()
        return {}
    end

    ---@diagnostic disable-next-line
    function event.pull(timeoutSeconds)
    end
end
